# لوحة التحكم على سيرفر v2 — حالة الربط

هذه اللوحة (نسخة v1) صارت تقرأ وتكتب على **سيرفر LinkUp v2** (NestJS + Postgres)
عبر `src/lib/v2Api.ts`. القاعدة التي اتُّبعت في كل خطوة:

> **الصفحات لم تُلمَس.** كل دالة في `src/services/admin.ts` حافظت على اسمها
> وشكلها، والتغيير كله تحت هذا السطر. ولا تُستدعى أي **Firebase Function** من هنا
> إطلاقاً، ولا تُكتب أي بيانات في قاعدة v1 (مرجع قراءة فقط).

---

## ما يحتاجه سيف قبل الاستخدام

| # | الخطوة | التفصيل |
|---|--------|---------|
| 1 | `VITE_V2_API_BASE_URL` | رابط الـAPI. الافتراضي `https://linkup-v2.onrender.com/api`؛ للتطوير المحلي `http://localhost:3000/api`. |
| 2 | **أول حساب مشرف** | لم يعد بالإمكان ترقية النفس من اللوحة (بقصد). مرة واحدة على القاعدة: `UPDATE users SET role='superadmin' WHERE email='…';` وبعدها كل الصلاحيات تُدار من صفحة «المشرفون». |
| 3 | `CORS_ORIGINS` على Render | يجب أن يشمل نطاق اللوحة، وإلا رفض المتصفح كل الطلبات. |
| 4 | مهاجرة `admin_logs` | لو لم تُنفَّذ بعد على الإنتاج: `npm run migration:run` — سجل العمليات وسجل البث يعتمدان عليها. |
| 5 | `REDIS_URL` | جلسات اللوحة و«النشطون» في البث تعتمد على Redis. بدونه: الجلسات لا تُتابَع (تُبلّغ `tracked:false` بصدق) والبث لـ«النشطون» لا يجد أحداً. |

## أين تعيش البيانات الآن

| المجال | المصدر | ملاحظات |
|-------|--------|---------|
| الدخول | `POST /identity/login` ثم `GET /admin/me` | السيرفر يقرر مَن مشرف، لا المتصفح. 401 = كلمة مرور، 403 = ليس مشرفاً. |
| المشرفون | `users.role` + `profile.adminPermissions/adminCountries` | لا يوجد جدول مشرفين منفصل: المشرف حساب. «حذف مشرف» = سحب الصلاحية لا حذف الشخص. |
| جلسات اللوحة | `/admin/sessions/*` (Redis) | الإخراج القسري بعدّاد نسخة لكل حساب، فيموت أي تبويب لم يره السيرفر. |
| سجل العمليات | `/admin/logs` | السطر يُوقَّع من التوكن — لا يمكن نسبته لشخص آخر. |
| كل الكتالوجات | `/admin/config/:id` (`config_docs`) | الحفظ **يدمج** (اقرأ→دمج عميق→اكتب) لأن الـPUT يستبدل المستند كاملاً. |
| المستخدمون | `/admin/users*` | الرصيد لا يُكتب كحقل: `/balance` (فرق) و`/balance/set` (قيمة مطلقة بقفل صف) وكلاهما بسجل معاملة + تدقيق. |
| السحوبات | `/admin/withdrawals*` | الإرجاع عند الرفض من السيرفر داخل نفس المعاملة. بيانات الدفع في قراءة الطلب الواحد فقط. |
| التحقق (KYC) | `/admin/kyc*` | الاسم القانوني والصور في قراءة الطلب الواحد فقط. الاعتماد يأخذ **معرّف الطلب** لا الـuid. |
| الغرف | `/admin/rooms` + `/rooms/:id/close` | الإغلاق يسقط الحالة الحيّة كذلك، فمن بالداخل يخرج فعلاً. |
| الوكالات | `/admin/agencies*` | توثيق + تعديل بيانات + مستوى الفترة. كوينز الوكالة للقراءة فقط هنا. |
| المنشورات | `/admin/posts*` | «حذف» = حالة `removed` (حذف ناعم) فيبقى الدليل. |
| البلاغات | `/admin/reports*` | المرفقات والأدلة في قراءة البلاغ الواحد فقط. |
| الإشعارات | `/admin/broadcast` + `/users/:id/notify` | البث صف حقيقي لكل مستلم بسقف 50 ألف؛ «النشطون» = المتصلون الآن فعلاً. |
| الإحصائيات | `/admin/overview` + `/admin/analytics` | استعلامات تجميعية على السيرفر من نفس سجل المال. |
| صفحة التحميل | `/app/release` (عامة) | لأن الصفحة مفتوحة للزوار بلا توكن. |

## أشياء تقول «غير متاح» بصراحة (بدل بيانات وهمية)

| الوظيفة | لماذا | البديل الآن |
|--------|-------|-------------|
| حذف حساب نهائياً | الحساب مرتبط بسجل معاملاته وسحوباته وعضويته؛ الحذف قرار مالك | «حظر» يوقف الوصول فوراً |
| حذف غرفة نهائياً | مرتبطة بالوكالة وسجل الهدايا | «إغلاق الغرفة» (وقابل للتراجع) |
| قائمة معاملات الألعاب لكل النظام | v2 له سجل واحد يُقرأ لكل مستخدم؛ لا نقطة تفحص كل الجدول | تبويب معاملات المستخدم |
| تعديل حالة معاملة يدوياً | صف السجل حكاية شيء حدث؛ تغيير حالته لا يحرّك المال معه | «شحن رصيد» يسجّل معاملة حقيقية |
| صفحة «العلاقات» | لا يوجد جدول relationships في v2 (الرسم الاجتماعي = متابعات + حجب) | — |
| «الإيرادات» في الرئيسية والرسم | مسار الشحن الحقيقي لم ينتقل بعد لسجل v2، فلا رقم حقيقي | العمود يعرض الكوينز المُنفقة (رقم حقيقي) |
| مركز الدعم | لا هوية دعم ولا مسار إداري للمحادثات في v2؛ حدود قراءة المحادثات قرار مالك | الرد معطّل برسالة واضحة |
| وسائط رسالة مُبلَّغ عنها | نفس السبب أعلاه | ما أرفقه المُبلِّغ يظهر في تفاصيل البلاغ |
| دقائق المكالمات في الرسم | لا تجميع لجلسات المكالمات على السيرفر بعد | تظل صفراً بلا تلوين |

## ✅ صفر كتابة في قاعدة v1 — وصفر Firebase Function

مسح كامل لكل ملفات `src` (‏2026-07-26): **لا `setDoc`/`updateDoc`/`addDoc`/`deleteDoc`،
ولا `httpsCallable`، ولا كتابة RTDB** في أي مكان من اللوحة. اللوحة لم تعد تستطيع تعديل
القاعدة القديمة إطلاقاً.

**وُصلت بالكامل على v2:** ربط عضو/مضيفة بوكالة (الدور يُقرَّر من الحساب: أنثى موثّقة =
مضيفة) · إزالة عضو (**تُرفض ما دام يملك كوينز عمل** — تلك أرباحه وصف العضوية هو محفظتها) ·
أقصى مايكات الوكالة + مقاعد الغرفة (سقف الوكالة يُنفَّذ على السيرفر، والحالة الحيّة
تُحدَّث بمعاملة compare-and-set فلا يضيع مايك يُؤخذ في نفس اللحظة) · إنشاء وكالة مباشرة
(بعضوية المالك معها) · أمير الوكلاء (منح/سحب/إعادة حساب شهري — والترتيب **من الدفتر** لا
من محفظة الوكالة التي تُفرّغها السحوبات) · مستوى فترة الوكالة · تعديل بيانات الوكالة.

**ثلاث ميزات ترفض بصراحة لأن الميزة نفسها لم تنتقل** (ولا تكتب في v1):

| الميزة | لماذا | البديل الآن |
|--------|-------|-------------|
| طلبات زيادة المايكات | لا جدول ولا مسار في التطبيق يقدّم الطلب على v2 | «أقصى مايكات الوكالة» و«مقاعد الغرفة» مباشرة |
| السهرات (طلبات/إيقاف) | نفس السبب — لا شاشة في التطبيق تُنشئ سهرة | «إغلاق الغرفة» يُنهي ما يجري فعلاً |
| موظفو التطبيق | التطبيق لا يعرف دور الموظف، فحفظه = بيان لا يظهر لأحد | صلاحيات «المشرفون» + «منح الامتيازات» |

**وأُوقفت صراحةً** أيضاً: حذف الوكالات/الكل/تنظيف اليتيم، وتعديل «الماس المكتسب» يدوياً
(حقل مال — البديل «تعديل الرصيد» المُسجَّل)، وردود مركز الدعم، وزر «بذور تجريبية».
و`resolveUserActiveAgencyId` كان يُصلح البيانات أثناء القراءة (يحذف دردشات يتيمة!) —
صار قراءة خالصة. وحُذف `addVipPointsAdmin` (كان يحسب مستوى VIP في المتصفح ويكتبه) —
نقاط VIP تُحسب على السيرفر في مسار الشحن نفسه.

## ما زال على Firestore (لم يُنقل بعد — قراءة فقط، بلا أي كتابة)

هذه الدوال داخل `services/admin.ts` وحدها بقيت على القاعدة القديمة، وكلها **قراءة**:

- تفاصيل الوكالة الكاملة والأعضاء المفصّلون + طلبات السهرات والمقاعد + أرباح
  الوكالة وتحليلاتها وسحوباتها (`getAgencyFullDetail`, `getAgencyMembers*`,
  `getAgencyPartyRequests`, `getAgencySeatRequests`, `getAgencyEarningsActivity`,
  `getAgencyAdminAnalytics`, `getAgencyWithdrawals`, `getAgencyRefundsAdmin`).
- طلبات فتح الوكالة والتحقق منها (`getAgencyApplications`, `getAgencyVerifications`)
  وقراراتها.
- موظفو التطبيق (`listPlatformStaffUsers`, `adminCreateStaffUser`, …) — مفهوم v1
  (دور موظف + إطار/شارة) وليس له مقابل في v2 بعد.
- أمير الوكلاء (`refreshMonthlyAgencyPrince`, `grantAgencyPrinceManual`, …).
- استهلاك المكالمات والمزوّد (`getCallUsageStats`, `getLiveKitUsage`) — و**LiveKit
  حُذف من v2 أصلاً**، المكالمات على Agora.
- تحليلات متقدّمة وقائمة المعاملات العامة (`getAdvancedAnalytics`,
  `getTransactions`) — تُقرأ من سجل v1؛ مقابلها في v2 هو `/admin/analytics`.
- جلسات دخول المستخدم (`getUserLoginSessions`) — v2 لا يتابع جلسات المستخدمين.

**الترتيب المقترح للجولة القادمة:** الوكالات (تفاصيل + أعضاء + طلبات) ← الموظفون
← مركز الدعم ← استهلاك المكالمات ← تحليلات متقدّمة.

## بوابة الجودة

```
npx tsc --noEmit -p tsconfig.json
```

يجب ألا يظهر أي خطأ خارج `src/landing/*` و`src/components/CrashGame.tsx`
(37 خطأً موروثاً من قبل هذا العمل، لا علاقة لها بالربط).
