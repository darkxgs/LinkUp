/**
 * طبقة الروم العائمة داخل التطبيق — بعد «احتفظ في الروم»
 */
import React from 'react';
import { FloatingRoomBubble } from './FloatingRoomBubble';

export function RoomFloatingOverlay() {
  return <FloatingRoomBubble />;
}
